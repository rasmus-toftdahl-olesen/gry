#cmakedefine GRY_INCLUDE_STATIC_WEB

#define GRY_STATIC_WEB_DIR "${GRY_STATIC_WEB_DIR}"

#define GRY_WEB_PORT ${GRY_WEB_PORT}
#define GRY_UDP_PORT ${GRY_UDP_PORT}

#cmakedefine GRY_NON_CONST_PION_PTRS
